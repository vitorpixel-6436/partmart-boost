# PartMart Boost - Requirements
# Зависимости для Python 3.11+

# === CORE DEPENDENCIES ===
# GPU Control
pynvml==11.5.0          # NVIDIA GPU management via NVML
nvidia-ml-py3==7.352.0  # Alternative NVIDIA library

# System Monitoring
psutil==5.9.5           # Cross-platform system and process utilities
wmi==1.5.1              # Windows Management Instrumentation (XMP enable)

# UI Framework
PyQt6==6.5.2            # Qt6 bindings for Python
PyQt6-Qt6==6.5.2        # Qt6 runtime
PyQt6-sip==13.5.2       # SIP module for PyQt6

# Networking
requests==2.31.0        # HTTP library (updates, telemetry)
urllib3==2.0.4          # urllib3 for requests

# === DEVELOPMENT DEPENDENCIES ===
# Testing
pytest==7.4.3           # Testing framework
pytest-cov==4.1.0       # Coverage plugin for pytest
pytest-mock==3.11.1     # Mock plugin for pytest
pytest-qt==4.2.0        # PyQt testing support

# Code Quality
pylint==3.0.2           # Static code analysis
black==23.11.0          # Code formatter
flake8==6.1.0           # Style guide enforcement
mypy==1.7.0             # Static type checker

# === OPTIONAL DEPENDENCIES ===
# Logging and Monitoring
loguru==0.7.2           # Better logging

# Configuration
pyyaml==6.0.1           # YAML config parser
toml==0.10.2            # TOML config parser

# Performance Profiling (dev only)
py-spy==0.3.14          # Sampling profiler
memory-profiler==0.61.0 # Memory profiling

# === BUILD DEPENDENCIES ===
# Packaging
pyinstaller==6.2.0      # Create standalone executables
setuptools==69.0.2      # Package setup
wheel==0.42.0           # Wheel package format

# === OPTIONAL: AMD GPU Support ===
# pyamdgpuinfo==2.1.6   # AMD GPU information (Linux only)
# Note: AMD control via OverdriveNTool.exe (Windows binary)

# === OPTIONAL: Intel Arc Support ===
# Currently no Python library available
# Control via Intel Graphics Command Center CLI
