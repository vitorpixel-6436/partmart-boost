# PartMart Boost Requirements
# Version: 0.3.5d_hotfix3 (Package 3.9a, Stage 7.7d_hotfix3)

# GUI Framework
PyQt6>=6.4.0

# Charting and visualization
pyqtgraph>=0.13.0
numpy>=1.24.0              # Required by pyqtgraph and data processing

# System monitoring
psutil>=5.9.0              # CPU, RAM, Disk monitoring

# GPU monitoring (NVIDIA)
# UPDATED: nvidia-ml-py is the official package (pynvml is deprecated)
nvidia-ml-py>=12.535.133   # NVIDIA GPU monitoring (official)

# Image processing (for FSR)
Pillow>=10.0.0             # Image handling for FSR processing

# Optional: AMD GPU monitoring
# pyadl>=0.1                # Uncomment for AMD GPU support

# Database
# sqlite3 is built-in to Python standard library

# Windows-specific (DLL injection)
# ctypes is built-in to Python standard library
# pywin32>=305              # Uncomment for advanced Windows API features

# Development dependencies (optional)
# pytest>=7.4.0
# coverage>=7.3.0
# black>=23.0.0
# flake8>=6.1.0
